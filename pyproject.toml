[project]
name = "shop"
version = "0.1.0"
description = "Учимся создавать магазин"
authors = [
    {name = "damodara",email = "jay108@mail.ru"}
]
readme = "README.md"
#requires-python = ">=3.12"
dependencies = [
    "redis (>=7.1.0,<8.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.dependencies]
python = "^3.10"
